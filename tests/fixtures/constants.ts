/**
 * Shared constants for test fixtures.
 * Ensures deterministic, repeatable data across unit, integration, and e2e.
 */

/** Fixed timestamp for all fixture dates (no time drift). */
export const FIXTURE_DATE = "2025-01-15T12:00:00.000Z" as const;
